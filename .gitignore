node_modules/
dist/
cdk.out/
.env
.env.*
*.js
*.d.ts
*.js.map
*.d.ts.map
!.eslintrc.cjs
!**/md.d.ts
# Next generates this at dev time and documents it as committable — clean
# checkouts need it for `tsc --noEmit` to resolve CSS module imports.
!frontend/next-env.d.ts
*.tsbuildinfo
.kiro/evidence/unknown-spec/

# Local secrets — NEVER commit (Stripe keys, etc.). Belong in Secrets Manager.
Stripe API KEYS
*stripe*key*
*.secret
secrets/
